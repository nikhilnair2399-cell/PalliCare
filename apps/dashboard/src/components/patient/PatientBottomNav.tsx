'use client';

// Bottom nav removed — patient interface is desktop-first like clinician dashboard.
// Mobile users use the sidebar overlay (hamburger menu).
export function PatientBottomNav() {
  return null;
}
